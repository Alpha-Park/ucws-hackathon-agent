services:
  - type: web
    name: genpark-social-shopping-agent
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "bot.app:app"
    envVars:
      - key: GENPARK_AGENT_DB
        value: /tmp/agent_state.db
